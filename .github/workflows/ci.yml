name: ci

on:
  pull_request:
  push:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: read

jobs:
  changes:
    runs-on: ubuntu-latest
    outputs:
      core_or_benchmark: ${{ steps.filter.outputs.core_or_benchmark }}
    steps:
      - uses: actions/checkout@v4
      - uses: dorny/paths-filter@v3
        id: filter
        with:
          filters: |
            core_or_benchmark:
              - 'rust_umap/**'
              - 'umap_rs/**'
              - 'benchmarks/**'
              - '.github/workflows/**'

  rust-fmt:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
        with:
          components: rustfmt
      - name: Cargo fmt check
        run: cargo fmt --manifest-path rust_umap/Cargo.toml --all --check

  rust-verify:
    needs: [rust-fmt]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
        with:
          components: clippy
      - uses: Swatinem/rust-cache@v2
        with:
          workspaces: rust_umap
      - name: Cargo clippy
        run: cargo clippy --manifest-path rust_umap/Cargo.toml --all-targets -- -D warnings
      - name: Cargo test
        run: cargo test --manifest-path rust_umap/Cargo.toml
      - name: Release binaries
        run: cargo build --release --manifest-path rust_umap/Cargo.toml --bin fit_csv --bin bench_fit_csv

  consistency-smoke:
    needs: [changes, rust-verify]
    if: needs.changes.outputs.core_or_benchmark == 'true'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
      - uses: Swatinem/rust-cache@v2
        with:
          workspaces: rust_umap
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Install Python benchmark deps
        run: python -m pip install --upgrade pip && python -m pip install -r benchmarks/requirements-bench.txt
      - name: Public implementation consistency smoke gate
        run: |
          python benchmarks/ci_consistency_smoke.py \
            --python-bin python \
            --gate-config benchmarks/gate_thresholds.json \
            --output-json consistency-smoke.json
      - name: Upload consistency smoke artifact
        uses: actions/upload-artifact@v4
        with:
          name: consistency-smoke
          path: consistency-smoke.json

  ann-e2e-smoke:
    needs: [changes, rust-verify]
    if: needs.changes.outputs.core_or_benchmark == 'true'
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
      - uses: Swatinem/rust-cache@v2
        with:
          workspaces: rust_umap
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Install Python benchmark deps
        run: python -m pip install --upgrade pip && python -m pip install -r benchmarks/requirements-bench.txt
      - name: ANN/e2e smoke gate (python_umap_learn vs rust_umap)
        run: |
          set -euo pipefail
          python benchmarks/ci_ann_smoke.py \
            --python-bin python \
            --gate-config benchmarks/gate_thresholds.json \
            --output-json ann-e2e-smoke.json
      - name: Upload ANN/e2e smoke artifact
        uses: actions/upload-artifact@v4
        with:
          name: ann-e2e-smoke
          path: ann-e2e-smoke.json

  wave1-smoke:
    needs: [changes, rust-verify]
    if: needs.changes.outputs.core_or_benchmark == 'true'
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
      - uses: Swatinem/rust-cache@v2
        with:
          workspaces: rust_umap
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Wave-1 reliability smoke gate
        run: |
          set -euo pipefail
          python benchmarks/ci_wave1_smoke.py \
            --manifest-path rust_umap/Cargo.toml \
            --output-json wave1-smoke.json
      - name: Upload Wave-1 smoke artifact
        uses: actions/upload-artifact@v4
        with:
          name: wave1-smoke
          path: wave1-smoke.json

  no-regression-smoke:
    needs: [changes, consistency-smoke, ann-e2e-smoke, wave1-smoke]
    if: needs.changes.outputs.core_or_benchmark == 'true'
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        metric: [euclidean, manhattan, cosine]
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: dtolnay/rust-toolchain@stable
      - uses: Swatinem/rust-cache@v2
        with:
          workspaces: rust_umap
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Install Python benchmark deps
        run: python -m pip install --upgrade pip && python -m pip install -r benchmarks/requirements-bench.txt
      - name: Resolve baseline ref
        id: baseline
        env:
          EVENT_NAME: ${{ github.event_name }}
          PR_BASE_SHA: ${{ github.event.pull_request.base.sha }}
          PUSH_BEFORE_SHA: ${{ github.event.before }}
        run: |
          set -euo pipefail
          if [[ "$EVENT_NAME" == "pull_request" && -n "${PR_BASE_SHA}" ]]; then
            BASE_REF="$PR_BASE_SHA"
          elif [[ "$EVENT_NAME" == "push" && -n "${PUSH_BEFORE_SHA}" && "${PUSH_BEFORE_SHA}" != "0000000000000000000000000000000000000000" ]]; then
            BASE_REF="$PUSH_BEFORE_SHA"
          else
            BASE_REF="HEAD~1"
          fi
          echo "base_ref=${BASE_REF}" >> "$GITHUB_OUTPUT"
      - name: Materialize baseline worktree
        run: |
          set -euo pipefail
          BASE_REF="${{ steps.baseline.outputs.base_ref }}"
          if [[ "$BASE_REF" =~ ^[0-9a-f]{40}$ ]]; then
            git fetch origin "$BASE_REF" --depth=1
          else
            git fetch origin main --depth=2
          fi
          git worktree add "$RUNNER_TEMP/umapers-baseline" "$BASE_REF"
      - name: Metric no-regression smoke gate
        run: |
          python benchmarks/ci_no_regression.py \
            --candidate-root "$GITHUB_WORKSPACE" \
            --baseline-root "$RUNNER_TEMP/umapers-baseline" \
            --metric "${{ matrix.metric }}" \
            --gate-config benchmarks/gate_thresholds.json \
            --output-json "no-regression-smoke-${{ matrix.metric }}.json"
      - name: Upload no-regression artifact
        uses: actions/upload-artifact@v4
        with:
          name: no-regression-smoke-${{ matrix.metric }}
          path: no-regression-smoke-${{ matrix.metric }}.json
